package com.cnc.algorithms;

import java.util.Map;


/**
 * Given the most commonly occurring pairs of word in a document
 * @author evharris
 *
 */
public class FindCommonWordsInString {

  /**
   * 
   * @param input
   * @param howMany
   * @return a map containing strings and how many times each occures in the given input.
   * the return map is filtered to only show strings that occur equal or more than 
   * the how many parameter
   */
   public Map<String, Integer> countPairs(String input, int howMany){
       throw new UnsupportedOperationException();
     
   }
  
}
